import { Carousel } from "@material-tailwind/react";
import { ProjectImageModel } from "../interfaces/projects/projectTypes";
import { IoExpandSharp } from "react-icons/io5";

interface CarouselProps {
  imageUrls: ProjectImageModel[];
}

export function CarouselCustomArrows({ imageUrls }: CarouselProps) {
  return (
    <>
      {/* {imageUrls.map((image) => { */}

      {/* })} */}
    </>
  );
}
